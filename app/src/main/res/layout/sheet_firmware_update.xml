<?xml version="1.0" encoding="utf-8"?>
<!-- Firmware update bottom sheet — mirrors iOS FirmwareUpdateSheetViewController:
     title, large %, 30-segment progress bar, status text, description and notes. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_bottom_sheet"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="16dp"
    android:paddingBottom="32dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-light"
        android:text="@string/firmware_update"
        android:textColor="@color/black"
        android:textSize="24sp" />

    <!-- Percentage (left) + status (right) -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp">

        <TextView
            android:id="@+id/percentLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:fontFamily="sans-serif-light"
            android:text="0%"
            android:textColor="@color/black"
            android:textSize="44sp" />

        <TextView
            android:id="@+id/statusLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignBottom="@id/percentLabel"
            android:layout_marginBottom="6dp"
            android:fontFamily="sans-serif"
            android:text="@string/preparing"
            android:textColor="@color/gray5"
            android:textSize="14sp" />
    </RelativeLayout>

    <!-- Segmented progress bar (segments added programmatically) -->
    <LinearLayout
        android:id="@+id/segmentContainer"
        android:layout_width="match_parent"
        android:layout_height="16dp"
        android:layout_marginTop="12dp"
        android:orientation="horizontal" />

    <TextView
        android:id="@+id/descriptionLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:fontFamily="sans-serif"
        android:textColor="@color/gray5"
        android:textSize="14sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:fontFamily="sans-serif"
        android:text="@string/firmware_update_notes"
        android:textColor="@color/gray5"
        android:textSize="14sp" />

</LinearLayout>
