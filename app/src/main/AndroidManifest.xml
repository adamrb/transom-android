<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- BLE permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Location permission (required for BLE scanning) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Self-update: launch the system installer for APKs downloaded from YOUR server -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Background sync: foreground service that keeps the BLE link alive while the app is in
         the background. API 34 requires the per-type permission next to FOREGROUND_SERVICE. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- API 33+: the service notification is invisible without this runtime permission -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Restart the service after a reboot / app update -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Settings > Battery optimization: ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:name=".PlaudBridgeApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlaudBridge"
        tools:targetApi="31">

        <!-- Onboarding -->
        <activity
            android:name=".ui.onboarding.WelcomeActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.onboarding.ServerSetupActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.onboarding.ScanningActivity"
            android:exported="false" />

        <activity
            android:name=".ui.onboarding.OnboardingSuccessActivity"
            android:exported="false" />

        <!-- Main -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:launchMode="singleTask" />

        <!-- Recording (dark translucent theme) -->
        <activity
            android:name=".ui.recording.RecordingActivity"
            android:exported="false"
            android:theme="@style/Theme.PlaudBridge.Recording" />

        <!-- Device Panel -->
        <activity
            android:name=".ui.home.DevicePanelActivity"
            android:exported="false" />

        <!-- File Detail -->
        <activity
            android:name=".ui.filedetail.FileDetailActivity"
            android:exported="false" />

        <!-- Custom vocabulary editor (Settings). adjustResize keeps Save above the keyboard. -->
        <activity
            android:name=".ui.settings.VocabularyActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Web dashboard (the server's own UI, from Settings; formerly the Library tab) -->
        <activity
            android:name=".ui.library.WebDashboardActivity"
            android:exported="false" />

        <!-- PackageInstaller session status for self-updates (never exported) -->
        <receiver
            android:name=".ui.update.UpdateResultReceiver"
            android:exported="false" />

        <!-- Background sync: keeps the process + BLE link alive (see DeviceConnectionService) -->
        <service
            android:name=".service.DeviceConnectionService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Restart the connection service after reboot / app update (system broadcasts only) -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
